package com.musiclibrary.player;

/**
 * Stato della riproduzione nel player.
 */
public enum PlaybackStatus {
    /** Nessuna riproduzione in corso. */
    STOPPED,
    /** Riproduzione in corso. */
    PLAYING,
    /** Riproduzione in pausa. */
    PAUSED
}
